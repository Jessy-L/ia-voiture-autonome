// algorithme de lerp
// lerp = linear interpolation
// Algorithme qui permet de calculer une position entre deux points
// en fonction d'un pourcentage

function lerp (A, B, t) {
    return A + (B - A) * t;
}

